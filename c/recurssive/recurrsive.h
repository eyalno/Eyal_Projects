#ifndef RECURSIVE_H
#define RECURSIVE_H



size_t fibbonaciRec(size_t _n);
size_t fibbonciIter(size_t _n);
int findMax(int*,size_t);
int reccursiveBinary(int *,int,int,int);
int iterativeBinary(int*,int,int);
void hanoi(int,Stack*,Stack*,Stack*);

#endif
